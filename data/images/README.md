Place the 36 H&E micrographs here (TIFF or PNG). File names encode the
experimental family and the replicate, e.g. `WT 3m.tif`, `WT 3m II.tif`,
`WT 3m III.tif`, `p62-2y_II.tif`, `RANKL_Tg_I.tif`. The family is the file
name without the trailing Roman numeral (underscores are read as spaces).
The images are available from the corresponding author on reasonable request.
